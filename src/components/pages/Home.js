import React from 'react';
import { Layout } from '../';

const Home = () => {
    return (
        <Layout>
            <div className='container'>Site Main</div>
        </Layout>
    );
};

export default Home;
